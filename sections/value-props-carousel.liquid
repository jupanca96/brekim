<div>
    <p>Esto es para ver el test</p>
    {%  comment  %}  

    <div class="profile">
        <img src="{{ section.settings.user_img | img_url: '300x300' }}" alt="">
        <h3>
        {{ section.settings.user_name }}
        </h3>
        <p>
            {{ section.settings.user_bio }}
        </p>
    </div>

    {% endcomment %}
    
</div>

{% schema %}
    {
        "name": "Profile",
    
        "settings": [
            {
            "type": "header",
            "content": "Texto header"
          }
        ],

        "blocks": [
            {
            "type": "row",
            "name": "icon",
            "settings": [
                {
                "type": "image_picker",
                "id": "image",
                "label": "icon_svg"
                },
                {
                "type": "text",
                "id": "text",
                "label": "text_icon"
                },
                {
                "type": "url",
                "id": "link",
                "label": "link"
                }
            ]
            }
        ],
    
        "presets": [
            {
                "category": "Custom content",
                "name": "User profile"
            }
        ]
    }
    {% endschema %}
    